
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>WONEUVWONE | Legendary Luxury Watches</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Montserrat:wght@400;500;600&display=swap" rel="stylesheet">
<style>
body{margin:0;font-family:'Montserrat',sans-serif;background:#080808;color:#fff;text-align:center;overflow-x:hidden;}
header{background:#0f0f0f;padding:35px 20px;border-bottom:4px solid #ffcc00;box-shadow:0 0 25px #ffcc00 inset;}
header h1{margin:0;font-size:3rem;color:#ffcc00;font-family:'Playfair Display',serif;letter-spacing:3px;}
.discount-code{margin-top:10px;font-size:1.1rem;color:#00ff99;letter-spacing:1px;}
.products{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:30px;margin:50px 20px;position:relative;z-index:1;}
.product{background:#111;border-radius:25px;padding:25px 15px;box-shadow:0 0 25px #ffcc00,0 0 15px #00ff99 inset;transition:transform 0.4s,box-shadow 0.4s;position: relative;overflow:hidden;}
.product:hover{transform:translateY(-10px) scale(1.02);box-shadow:0 0 50px #ffcc00,0 0 25px #00ff99 inset;}
.product img{width:100%;border-radius:15px;margin-bottom:15px;filter:brightness(0.95);transition:transform 0.4s,filter 0.4s;}
.product:hover img{transform:scale(1.07) rotate(-1deg);filter:brightness(1.05);}
.product h2{font-size:1.5rem;margin:8px 0;color:#ffcc00;font-family:'Playfair Display',serif;}
.product p{font-size:1rem;margin:5px 0;text-decoration: line-through;color:#888;}
.discount-price{font-size:1.3rem;color:#00ff99;font-weight:bold;margin:5px 0;position:relative;}
.discount-price::after{content:' ✨';animation:sparkle 2s infinite;}
@keyframes sparkle{0%,100%{opacity:0;transform:translateY(0) rotate(0deg);}50%{opacity:1;transform:translateY(-2px) rotate(15deg);}}
.material,.unique,.investment-value,.scarcity{font-size:0.9rem;margin:2px 0;color:#aaa;font-weight:500;}
.unique{color:#ffcc00;font-weight:600;}
.investment-value{color:#00ff99;font-style:italic;}
.scarcity{color:#ff3333;font-weight:bold;}
.verify-section{margin:50px 20px;}
.verify-section input{padding:14px;font-size:1rem;width:220px;border-radius:6px;border:none;margin-right:10px;background:#222;color:#fff;}
.verify-section button{padding:14px 20px;font-size:1rem;border-radius:6px;background:#ffcc00;border:none;cursor:pointer;font-weight:bold;transition:0.3s;}
.verify-section button:hover{background:#ffdd33;}
#verify-result,#order-result{margin-top:10px;font-weight:bold;}
footer{margin-top:40px;padding:25px;background:#0f0f0f;color:#888;border-top:4px solid #ffcc00;position:relative;z-index:1;}
.photo-section img{border-radius:10px;margin-bottom:5px;width:100%;}
.stock-counter{font-size:0.85rem;color:#00ff99;margin-top:5px;}
</style>
</head>
<body>

<header>
<h1>WONEUVWONE</h1>
<p class="discount-code">Collector’s Secret Code: 90OFF</p>
</header>

<div class="products" id="products-container"></div>

<div class="verify-section">
<h3>Verify Your Watch’s Authenticity</h3>
<input type="text" id="verify-code" placeholder="Enter your unique code">
<button onclick="verifyWatch()">Verify</button>
<p id="verify-result"></p>
</div>

<div class="verify-section">
<h3>Place Your Order</h3>
<input type="text" id="order-code" placeholder="Enter discount code">
<input type="text" id="order-name" placeholder="Full Name">
<input type="text" id="order-address" placeholder="Shipping Address">
<input type="number" id="order-quantity" placeholder="Quantity" min="1">
<button onclick="placeOrder()">Order Now</button>
<p id="order-result"></p>
<button onclick="downloadOrders()">Download Order Log (CSV)</button>
</div>

<footer>© 2025 WONEUVWONE | Handmade, One-of-One, Historical Investment Grade</footer>

<script>
const totalWatches=75;
const productsContainer=document.getElementById('products-container');
const verificationCodes=[];
let orders=[];
let inventory = Array(totalWatches).fill(1000); // Starting stock

// Generate products
for(let i=0;i<totalWatches;i++){
  const productName=`Watch ${i+1}`;
  const code=`WONE${Math.random().toString(36).substring(2,8).toUpperCase()}${i+1}`;
  verificationCodes.push(code);

  const productHTML=`
    <div class="product">
      <img src="https://via.placeholder.com/220?text=Luxury+Watch" alt="${productName}">
      <h2>${productName}</h2>
      <p>$1111</p>
      <div class="discount-price">$111 <span style="font-size:0.8rem;color:#ffcc00;">Collector's Special</span></div>
      <div class="material">Material: Stainless Steel</div>
      <div class="unique">Handmade, One-of-One</div>
      <div class="investment-value">Historical Luxury Investment</div>
      <div class="scarcity">Only 1 in existence — Exclusive!</div>

      <div class="stock-counter" id="stock-${i}">Stock Available: ${inventory[i]}</div>

      <div class="photo-section">
        <p style="color:#aaa; font-size:0.9rem;">Making of the Watch:</p>
        <img src="https://via.placeholder.com/200x120?text=Process+Photo" alt="Making of Watch">
        <p style="color:#aaa; font-size:0.9rem;">Logo: <br> WONE <br> &nbsp;&nbsp;&nbsp;UV<br> WONE</p>
        <img src="https://via.placeholder.com/100x50?text=WONE+UV+Logo" alt="Logo">
      </div>

      <div style="font-size:0.8rem;color:#888;margin-top:5px;">Your Code: ${code}</div>
    </div>
  `;
  productsContainer.innerHTML+=productHTML;
}

// Verify watch
function verifyWatch(){
  const input=document.getElementById('verify-code').value.trim().toUpperCase();
  const result=document.getElementById('verify-result');
  if(verificationCodes.includes(input)){
    result.style.color='#00ff99';
    result.innerText='✅ Verified! You own a historical, one-of-one WONEUVWONE investment.';
  }else{
    result.style.color='#ff3333';
    result.innerText='❌ Invalid code. Check again.';
  }
}

// Place order
function placeOrder(){
  const codeInput=document.getElementById('order-code').value.trim().toUpperCase();
  const name=document.getElementById('order-name').value.trim();
  const address=document.getElementById('order-address').value.trim();
  const quantity=parseInt(document.getElementById('order-quantity').value);

  const result=document.getElementById('order-result');

  if(codeInput!=='90OFF'){ result.style.color='#ff3333'; result.innerText='❌ Invalid discount code!'; return; }
  if(!name||!address||quantity<1){ result.style.color='#ff3333'; result.innerText='❌ Fill all fields!'; return; }

  orders.push({name,address,quantity,date:new Date().toLocaleString()});
  result.style.color='#00ff99';
  result.innerText=`✅ Order placed! Quantity: ${quantity} | Total: $${quantity*111}`;

  for(let i=0;i<inventory.length;i++){
    inventory[i]-=quantity;
    if(inventory[i]<0) inventory[i]=0;
    document.getElementById(`stock-${i